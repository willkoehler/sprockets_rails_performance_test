en:
  simple_form:
    "yes": 'Yes'
    "no": 'No'
    required:
      text: 'required'
      mark: '*'
      # You can uncomment the line below if you need to overwrite the whole required html.
      # When using html, text and mark won't be used.
      # html: '<abbr title="required">*</abbr>'
    error_notification:
      default_message: "Please correct fields with errors below:"
    # Labels and hints examples
    # labels:
    #   defaults:
    #     password: 'Password'
    #   user:
    #     new:
    #       email: 'E-mail to sign in.'
    #     edit:
    #       email: 'E-mail.'
    # hints:
    #   defaults:
    #     username: 'User name to sign in.'
    #     password: 'No special characters, please.'

  helpers:
    submit:
      update: "Save"
      updating: "Saving"
      category:
        create: "Create"
        update: "Save"
      ops_audit_category:
        create: "Create"
        update: "Save"
      ops_audit_standard:
        create: "Create"
        creating: 'Creating'
      # Button label examples
      # update: "Save %{model}"
      # updating: "Saving %{model}"
      # password_reset:
      #   submit: "Reset my password"
      #   submitting: "Resetting password..."
      # surveillance_note:
      #   create: "Add Note"
      #   creating: "Adding..."
      #   update: "Save"
      #   updating: "Saving..."
      # video:
      #   update: "Save Video Info"
      #   updating: "Saving Video Info..."
      # highlight:
      #   create: "Add Highlight"
      #   creating: "Adding Highlight..."
